{
  "name": "@waterlog/authorizer",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.cjs",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --platform=node --bundle --minify --target=node18 --outfile=dist/index.cjs",
    "build:dev": "esbuild src/index.ts --platform=node --sourcemap --packages=external --bundle --target=node18 --outfile=dist/index.cjs",
    "dev": "pnpm build:dev --watch",
    "test": "pnpm run test:unit --run",
    "test:unit": "vitest -c vitest.config.unit.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@waterlog/utils": "workspace:*",
    "aws-lambda": "^1.0.7",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0",
    "node-cache": "^5.1.2",
    "tsconfig": "workspace:*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/jsonwebtoken": "^9.0.7",
    "esbuild": "^0.20.2",
    "lambda-local": "^2.2.0",
    "mock-jwks": "^3.2.2"
  }
}
