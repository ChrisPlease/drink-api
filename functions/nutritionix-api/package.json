{
  "name": "@waterlog/nutritionix-api",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.cjs",
  "scripts": {
    "build": "esbuild src/index.ts --platform=node --minify --bundle --target=node18 --outfile=dist/index.cjs",
    "build:dev": "esbuild src/index.ts --platform=node --sourcemap --bundle --target=node18 --outfile=dist/index.cjs",
    "dev": "pnpm build:dev --watch",
    "test": "pnpm run test:unit --run",
    "test:unit": "vitest -c ./vitest.config.unit.mts",
    "watch": "pnpm build:dev --watch"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "esbuild": "^0.20.2",
    "nodemon": "^3.1.7"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@waterlog/utils": "workspace:*",
    "aws-lambda": "^1.0.7",
    "undici": "^6.20.1"
  }
}
