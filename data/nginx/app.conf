server {
  listen 80;
  server_name api.waterlog.dev;

  location / {
    return 301 https://$host$request_uri
  }
}

server {
  listen 443 ssl;
  server_name api.waterlog.dev;

  location / {
    proxy_pass http://api.waterlog.dev;
  }
}
