{
  "name": "@waterlog/redis",
  "version": "1.0.0",
  "description": "",
  "main": "dist/redis.cjs",
  "type": "commonjs",
  "scripts": {
    "build": "esbuild redis.ts --platform=node --minify --bundle --target=node20 --outfile=dist/redis.cjs",
    "build:dev": "esbuild redis.ts --platform=node --sourceMap --bundle --target=node20 --outfile=dist/redis.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.17.9",
    "esbuild": "^0.20.2"
  },
  "dependencies": {
    "@keyv/redis": "^4.0.2",
    "keyv": "^5.2.1"
  }
}
